// Crie uma lista de nomes e use um for para exibir cada nome.

let nomes = ["Gervasio", "Bernardo", "Chavane", "Yons", "Sheldon"];
for (let i = 0; i < nomes.length; i++) {
  console.log(nomes[i]);
}
